server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    # ========== gateway配置 ==========
    gateway:
      discovery:
        locator:
          enabled: false
      routes:
        - id: user-service
          uri: http://user-service:8081
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=0

# CORS配置已在CorsConfig.java中配置

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    gateway:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    com.vibecoding: DEBUG
